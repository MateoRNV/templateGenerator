{
  "name": "template-generator",
  "version": "1.0.0",
  "description": "CLI tool to generate SQL scripts from CSV files",
  "type": "module",
  "scripts": {
    "generate": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.7.0"
  }
}
